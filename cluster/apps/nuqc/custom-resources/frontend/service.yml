apiVersion: v1
kind: Service
metadata:
  name: nuqc-frontend
spec:
  type: ClusterIP
  selector:
    app: nuqc-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
