apiVersion: v1
kind: Service
metadata:
  name: nqc-frontend
spec:
  type: ClusterIP
  selector:
    app: nqc-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
