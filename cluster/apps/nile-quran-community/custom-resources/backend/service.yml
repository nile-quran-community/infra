apiVersion: v1
kind: Service
metadata:
  name: nqc-backend
spec:
  type: ClusterIP
  selector:
    app: nqc-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
